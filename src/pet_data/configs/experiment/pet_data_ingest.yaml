defaults:
  - /_global_/defaults
  - /dataset@_global_.dataset: vision_frames
  - _self_

recipe:
  recipe_id: pet_data_ingest
  description: "pet-data smoke recipe — just loads the dataset plugin"
  scope: single_repo
  owner_repo: pet-data
  schema_version: "2.0.0"
  stages:
    - name: load
      component_registry: evaluators
      component_type: pet_infra.noop_evaluator
      inputs:
        dataset:
          ref_type: dataset
          ref_value: ${dataset.type}
      config_path: experiment/pet_data_ingest
      depends_on: []
  variations: []
  produces: []
  default_storage: local
  required_plugins:
    - pet_data
